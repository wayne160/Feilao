[project]
name = "feilao-backend"
version = "0.1.0"
description = "Feilao FastAPI backend"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.2",
    "uvicorn[standard]>=0.51.0",
    "python-escpos[win32]>=3.1",
    "python-dotenv>=1.2.2",
]
